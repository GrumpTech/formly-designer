import { EnvironmentSettings } from './settings';

export const environment: EnvironmentSettings = {
  storage: 'local',
  storagePrefix: 'formly_designer_',
  applyRedirect: true,
  useSettings: true,
};
